spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: root
    password: root
    url: jdbc:mysql://192.168.113.129/gulimall_ums?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=false&rewriteBatchedStatements=true
    username: xujunyang

#配置mapper.xml的路径
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 8000
